{
  "name": "company-research-agent-tooling",
  "version": "0.1.0",
  "private": true,
  "description": "Dev tooling (commitlint + husky) para company-research-agent. El backend vive en backend/ con uv.",
  "scripts": {
    "prepare": "husky"
  },
  "devDependencies": {
    "@commitlint/cli": "^19.6.0",
    "@commitlint/config-conventional": "^19.6.0",
    "husky": "^9.1.7"
  }
}
